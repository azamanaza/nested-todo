import { DoneItemsPipe } from './done-items.pipe';

describe('DoneItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new DoneItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
